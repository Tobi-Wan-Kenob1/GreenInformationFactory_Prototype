{
  "name": "greeninformationfactory-browser-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Browser tests for the Policy & Grant Finder (docs/finder). Run with `npm test` from this directory, or `node tests/browser/run.js` from the repo root.",
  "scripts": {
    "test": "node run.js"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
